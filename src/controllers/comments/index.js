const createComment = require('./create')
const deleteComment = require('./delete')
const listComments = require('./lists')

module.exports = {
    createComment,
    deleteComment,
    listComments
}